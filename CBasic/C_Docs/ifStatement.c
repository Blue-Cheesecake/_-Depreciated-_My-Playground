#include <stdio.h>

// if, else if, else statements

int main()
{
    int a = 10, b = 20;
    if ((a == 10) && (b == 20))
    {
        printf("a = 10 b = 20 True\n");
    }
    if ((a < 10) || (b >= 20))
    {
        printf("True\n");
    }
    return 0;
}
